
def top_k(lst, k):
    ls = []
    for i in range(k):
        ls.append(max(lst))
        lst.remove(max(lst))
    return ls



def test():
    assert
    assert
    assert
    assert
    assert
    '''
    It should also detect the exception values in the following code.
    '''
    assert
    assert
    assert
    '''
    It should also detect the exception values in the following code.
    '''
    assert
    assert
    assert

'''
### Third Question: 
Edit the following code to solve the problem of overflow.
'''
def count_ones():
    print(bin(int("1" * 110000)))
    print(bin(int("1" * 110001)))

count_ones()
'''
Expect a typeError:
